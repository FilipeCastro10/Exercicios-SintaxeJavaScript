/*
 * Podemos recriar a pasta node_modules a partir do descritor package.json
 *
 * Para instalar as dependencias de um projeto recem clonado usaremos o comando
 * 'npm install' dentro da pasta onde está presente o arquivo package.json
 * 
 * Todas as dependencias do projeto, e que estão descrita em package.json, serão
 * baixadas e instaladas.
 * Por isso não é necessário fazer o controle de versão da pasta node_modules. 
 * Qualquer um que der um clone no projeto poderá replicar as dependencias com 
 * o npm install.
 *
 *
 * 
 */